import {SCOBY_BE} from './env';

export const SERVER_ROOT = `https://${SCOBY_BE}`;

export const API_ROOT = `${SERVER_ROOT}`;

export const WEBSOCKET_URL = `wss://${SCOBY_BE}`;
// export const WEBSOCKET_URL = `wss://${SCOBY_BE}/ws`;
